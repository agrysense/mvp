/**
 * General purpose configuration file. Used from the below list of scripts:
 * - /agClientCore/login/html/login.hml
 *
 */

/*
 * Use the below to specify when to redirect to upon successful login
 * Make sure this script is imported into /agClientCore/login/html/login.html
 */
const redirectTarget = "/agApps/ppf/ppfClient/index.html";